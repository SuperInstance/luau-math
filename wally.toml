[package]
name = "superinstance/luau-math"
version = "0.1.0"
registry = "https://github.com/UpliftGames/wally-index"
realm = "shared"
license = "MIT"
description = "Core math library for Roblox games — symmetry groups, sequences, and rhythm math"

exclude = ["tests/*"]

[dependencies]
